{
  "name": "abstgit",
  "version": "0.1.2",
  "description": "A set of helper functions to use git",
  "homepage": "https://github.com/micziz/abstgit#readme",
  "main": "bundle.js",
  "type": "module",
  "scripts": {
    "lint": "yarn run eslint src --fix",
    "prettier": "yarn run prettier --write",
    "format": "yarn lint && yarn prettier"
  },
  "packageManager": "yarn@3.2.2",
  "devDependencies": {
    "eslint": "^8.22.0",
    "eslint-config-prettier": "^8.5.0",
    "prettier": "2.7.1"
  }
}
